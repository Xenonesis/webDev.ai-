{
  "buildCommand": "vite build --config vite.config.vercel.ts && node scripts/generate-index.js",
  "outputDirectory": "build/client",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
