[build]
  command = "npm install && npm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "18.18.0"
  NODE_ENV = "development"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
